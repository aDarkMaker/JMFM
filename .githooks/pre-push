#!/usr/bin/env bash
# Version bump lives in the `git push` alias wrapper (.githooks/push), which
# bumps before pushing so the push is a single clean fast-forward.
# This hook exists only to keep the hooks dir consistent; it never bumps.
set -euo pipefail
exit 0
