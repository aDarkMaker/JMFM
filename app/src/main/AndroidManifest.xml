<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 允许网络访问 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.JMFMobile"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JMFMobile.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 自定义的 PDF 列表界面（与主题风格一致） -->
        <activity
            android:name=".ui.viewer.PdfListActivity"
            android:exported="false"
            android:theme="@style/Theme.JMFMobile.NoActionBar" />
        <!-- 注册 ErrorActivity -->
        <activity
            android:name=".ui.viewer.ErrorActivity"
            android:exported="false"
            android:theme="@style/Theme.JMFMobile.NoActionBar" />
        <activity android:name=".PDFViewerActivity" android:theme="@style/Theme.JMFMobile.NoActionBar" />
        <!-- 注册设置页面 -->
        <activity android:name=".ui.settings.SettingsActivity" android:theme="@style/JMF_Settings_Dark_NoActionBar" />
        <activity android:name=".ui.viewer.BookViewerActivity" />
        <!-- FileProvider 用于共享应用私有文件目录给文件管理器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>